"use strict";

//Practice exercise 6.8
//Create a variable and store itm into a value
let varFunction = function (arg1) {
    //Output the the provied argument to the console
    console.log(arg1);
}

//Passing an argument to the function
varFunction("Katlego");

//Creating the same function as a normal function declaration
function normFunction2(arg1) {
    console.log(arg1);
}
//Passing an argument to the function
normFunction2("Maredi");